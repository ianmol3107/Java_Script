let count=4
console.log(count)
// let count=5    we can't declared any variable twice in program
count=10       // we can only modified it, both having same variable NAMES and we have changing the VALUES like -- 4 to 10.
console.log(count)
function display()
{
    let count=40  // we can declared again only in function! and this variable is totally different from other one.
    console.log(count)
}

display()